FROM node:20-alpine
WORKDIR /app
COPY package.json pnpm-lock.yaml ./
RUN npm install -g pnpm && pnpm install --frozen-lockfile
COPY src/server/socket.ts ./src/server/socket.ts
COPY tsconfig.json ./
EXPOSE 3001
CMD ["pnpm", "dev:socket"]
