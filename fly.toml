app = "jpmirai-socket"
primary_region = "nrt"

[build]
  dockerfile = "Dockerfile.socket"

[http_service]
  internal_port = 3001
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
